proces enter Fibonacci(enter: n) fer:
    enter: retornValue = 0 ç
    si(n < 2) fer:
        retornValue = n ç
    fisi
    sino fer:
        retornValue = (Fibonacci(n - 1) + Fibonacci(n - 2)) ç
    fisino

    retorna retornValue ç
fi

proces Calçot() fer:
    enter: iterador ç
    enter resultat ç
    per iterador de 0 fins 10 sumant nombre fer:
        resultat = Fibonacci(iterador) ç
        mostra(resultat) ç
    fi
    enter: valorFibonacci = Fibonacci(12) ç
    mostra("El valor es: " + valorFibonacci) ç
fi
