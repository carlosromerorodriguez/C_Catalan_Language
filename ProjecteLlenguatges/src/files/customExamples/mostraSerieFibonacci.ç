proces enter Fibonacci(enter: n) fer:
    enter: retornValue = 0 ç
    si(n < 2) fer:
        retornValue = n ç
    fisi
    sino fer:
        retornValue = Fibonacci(n - 1) + Fibonacci(n - 2) ç
    fisino

    retorna retornValue ç
fi

proces Calçot() fer:
    enter: iterador = 0 ç
    enter: resultat = 0 ç
    enter: numero = 0 ç
    per iterador de 0 fins 10 sumant 1 fer:
        resultat = Fibonacci(iterador) ç
        mostra("Fibonacci de: " + iterador + " = " + resultat + "\n") ç
    fi
fi